import { prisma } from "@/core/infrastructure/database/prisma";
export default async function RegionPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const region = await prisma.serviceRegion.findUnique({ where: { slugURL: slug }, include: { faqs: true } }); return <section className="mx-auto max-w-5xl px-4 py-12"><h1 className="text-4xl font-black">{region?.name ?? slug}</h1><p className="mt-4 text-muted-foreground">{region?.description}</p>{region?.faqs.map((faq) => <details key={faq.id} className="mt-4 rounded-xl border p-4"><summary>{faq.question}</summary><p>{faq.answer}</p></details>)}</section>; }
